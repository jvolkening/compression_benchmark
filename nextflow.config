docker.enabled = true
process.executor = 'awsbatch'
process.queue = 'medium-vanilla'
process.container = 'continuumio/miniconda3'
process.errorStrategy = 'retry'
process.maxErrors = 3

// set minimal defaults
process.cpus = 1
process.memory = '4GB'

aws {
    region = 'us-east-2'
}
